server:
  port: 5000

spring:
  cloud:
    config:
      name: config
      server:
        git:
          uri: https://github.com/maximan81/config-files.git
          default-label: master
        bootstrap: true
  application:
    name: config